<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
</head>
<body>
    <%- text.chapter %>
    <br>
    <%- text.text %>
    <br>
        <% for(let i = 0; i <= options.length - 1; i++ ) { %>
            <form action="/story" method="post">
                <input type="hidden" name="idText" value="<%= options[i].id_followup_text %>">
                <input type="submit" value="<%= options[i].options %>">
            </form>
        <% } %>
</body>
</html>